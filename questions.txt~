1. sends a message to only the current client. It tells that client who it is.
   send sends to only the current client where as sendAll sends to all clients
2. stdout
3. changed the clients id, which when generated is an int, to a string so that
   it can be easily
4. it is not. this is because the id in generated by incrementing count, it is
   never decremented thus client ids are not recycled
5. 9, 12, 13, 21, 22, 23 (the start of the while loop that listens for a
   connetion
6. halp???
7. 
